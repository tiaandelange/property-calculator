import { HeroReportStack } from "./hero/HeroReportStack";

/** Decorative stacked PDF pages for the public Reports landing hero. */
export function ReportsLandingHeroMockup() {
  return <HeroReportStack />;
}
